# ---------------- Core App ----------------
streamlit==1.38.0
requests==2.32.3
typing-extensions>=4.10.0

# ---------------- Audio / Media ----------------
numpy==1.26.4
pydub==0.25.1
av==12.2.0
sounddevice>=0.4.6
torchaudio>=2.3.0

# ---------------- Speech / ASR ----------------
faster-whisper==1.0.3
vosk==0.3.45

# ---------------- UI / Input ----------------
streamlit-mic-recorder==0.0.8

# ---------------- (Optional / Experimental) ----------------
# Only needed if you still use streamlit-webrtc components
# streamlit-webrtc==0.47.7

# ---------------- OpenAI (GPT features) ----------------
openai==1.43.0
